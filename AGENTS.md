# 去你的广告维护约束

本文件适用于整个仓库。修改前先阅读 `README.md`、`CONTRIBUTING.md`、`SECURITY.md` 和 `docs/ARCHITECTURE.md`。

## 产品边界

- 保持包名 `org.adguardian.app`，最低 Android 11，compileSdk/targetSdk 37。
- 不添加 `INTERNET`、悬浮窗、忽略电池优化、全量应用查询、root、Shizuku 或修改系统安全设置权限。
- 不承诺永久后台、100% 拦截、广告零帧出现或禁用其他应用的传感器。
- 不重新加入手动学习、流程教学、录制管理或 CLASS 课表增强。
- 保留五套原始 PNG 启动图标；品牌资源变更必须单独说明并审查。
- 用户界面只显示可理解的产品状态，不暴露调试术语。

## 自动处理安全

- 明确且唯一的安全关闭目标可以立即执行，不增加统一等待。
- 不盲点坐标、不通用按返回键，不自动操作支付、银行、权限、隐私、确认或奖励领取控件。
- 用户输入、无关包切换或前台所有权变化必须清除一次性跨应用证据。
- 阻塞读取、原生点击和异步手势前后都要重新核对前台包、窗口和设置状态。
- 用户输入只取消挂起动作，不重置规则已消耗次数、冷却或匹配时限。
- 失败目标按目标隔离；不得换引擎、换窗口或等待超时后重复触发。
- 已接受的动作必须留下真实记录，不得把“点击已接受”描述为“广告已关闭”。

## 规则与来源

- LTT 三份原始 JSON 保持冻结哈希。
- GKD 只打包广告类别；补充来源只允许经审查的应用内广告组。
- 禁止引入补充全局规则、银行/支付目标或组键冲突。
- 规则更新必须重建快照、二进制夹具和选择器语料，并通过真实解析测试。
- 保留 `third_party/` 中的许可、README、来源提交和哈希证明。

## 签名与发布

- 保持现有包名与签名身份不变，避免已安装版本失去升级兼容性。
- 发布前必须通过项目校验、Python 测试、GKD 运行时测试、Android 宿主测试、Release 构建和 Release Lint。
- Lint 完整报告必须先上传再执行门禁；报告缺失或损坏视为失败。
- APK 上限为 50 MiB，并必须生成 SHA-256 校验文件和签名验证记录。
