# 安全策略

## 支持范围

当前只对最新 Release 和 `main` 分支提供安全修复。旧版本可能包含已知问题，不建议继续使用。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交报告。不要在公开 Issue、Discussion 或 Pull Request 中披露可利用细节、签名材料、个人信息或第三方应用截图中的敏感内容。

报告中请包含：受影响版本、Android 版本、设备型号、复现步骤、实际影响，以及可安全分享的最小证据。维护者确认前请勿公开。

## 高风险范围

以下问题会被优先处理：

- 辅助功能误操作支付、银行、权限、隐私或确认控件；
- 跨应用状态污染、重复点击或失去前台归属校验；
- 未经声明的联网、数据上传或敏感信息持久化；
- APK 签名、发布工作流或依赖供应链被篡改；
- 规则来源、许可或完整性校验失效。

## 签名说明

发布流程会在上传前验证 APK 签名。若怀疑签名材料泄露，应立即暂停发布，并评估 Android 签名密钥轮换方案。
